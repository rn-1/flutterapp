import 'dart:ffi';

import 'package:flutter/material.dart';

class FillDetailsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Fill Details Screen'));
  }
}